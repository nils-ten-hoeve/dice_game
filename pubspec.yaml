name: dice_game
description: "A private implementation of Qwixx."
publish_to: 'none' # Remove this line if you wish to publish to pub.dev
version: 1.0.0+1

environment:
  sdk: ^3.6.0

dependencies:
  flutter:
    sdk: flutter
  collection: ^1.19.0
  undo: ^1.5.0
  font_awesome_flutter: ^10.8.0
  url_launcher: ^6.3.1
  bootstrap_icons: ^1.11.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
  flutter_launcher_icons: ^0.14.2

flutter:
  uses-material-design: true

